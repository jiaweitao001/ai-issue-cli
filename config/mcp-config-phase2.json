{
  "mcpServers": {
    "github-issue-fetcher": {
      "command": "node",
      "args": ["../skills/github-issue-fetcher/index.js"],
      "tools": ["*"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "report-validator": {
      "command": "node",
      "args": ["../skills/report-validator/index.js"],
      "tools": ["*"]
    }
  }
}
