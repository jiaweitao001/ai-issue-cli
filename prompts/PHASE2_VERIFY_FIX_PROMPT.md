# Phase 2 Verify Fix Prompt

You are fixing a previously committed solution after local HashiCorp PR-CI
verification failed.

Make the smallest issue-scoped correction that addresses every failed gate.
Do not revert unrelated work from the prior solution attempt. If generated or
vendor diff is shown, reproduce the appropriate source change and commit the
resulting generated files as part of the fix.

Create a commit for your fix before finishing.
